print = prelude::io::print
add = prelude::math::add
mul = prelude::math::mul
pair a b f = f a b
first p = p (\x y -> x)
second p = p (\x y -> y)

infix 2 + a b = add a b
infix 3 * a b = mul a b
infix 1 right $ a b = a b

a b c = b + 3
d e = e + 4
main = print $ d $ a 4 5
