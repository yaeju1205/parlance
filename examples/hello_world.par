import "examples/a.par"

print = prelude::io::print
add = prelude::math::add
mul = prelude::math::mul

infix 2 + a b = add a b
infix 3 * a b = mul a b
infix 1 right $ a b = a b

pair x y f = f x y
first p = let
    first = \x y -> x
in
    p $ first

second p = p $ \x y -> y

main = print $ a
